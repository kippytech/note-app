import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "brainy - notes",
};

function Notes() {
  return (
    <div>
      <div>Notes</div>
    </div>
  );
}

export default Notes;
